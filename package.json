{
  "name": "vocoby",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py'",
    "build:dictionary": "python3 scripts/build_dictionary.py && python3 scripts/build_dictionary.py data/most-1000.csv --output data/most-1000",
    "start": "python3 -m http.server 8000"
  }
}
